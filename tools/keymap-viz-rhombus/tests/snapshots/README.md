# Snapshot fixtures

`legend_smoke.json` — two-cell `legend/v0` used only by `test_core_legend_parse.rhm`
to exercise JSON → Cell parsing (including optional `wash`). Not an authoring
source; real legends come from Nickel export at viz time.

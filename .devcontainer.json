{
  "name": "smart-keymap",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "overrideCommand": false,
  "customizations": {
    "vscode": {
      "extensions": []
    }
  },
  "postCreateCommand": "just --version && rustc --version && riscv-none-elf-gcc --version | head -1 && nickel --version"
}

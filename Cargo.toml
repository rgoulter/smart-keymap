[package]
name = "smart_keymap"
version = "0.1.0"
edition = "2021"

[features]
std = []

[lib]
name = "smart_keymap"
crate-type = ["staticlib"]

[dependencies]
libc = "0.2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
